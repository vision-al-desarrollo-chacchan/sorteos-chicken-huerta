"use client";
import {useCallback,useEffect,useState} from "react";
type Registro={id:number;nombre:string;dni:string;celular:string;operacion:string;cantidad:number;monto:number;estado:string;creado:string;comprobanteKey:string;tickets:string};
export default function Admin(){const[rows,setRows]=useState<Registro[]>([]),[loading,setLoading]=useState(true),[error,setError]=useState("");const load=useCallback(async()=>{setLoading(true);const r=await fetch("/api/admin");if(r.status===401){setError("Inicia sesión con la cuenta administradora para continuar.");setLoading(false);return}const d=await r.json();setRows(d.registros||[]);setLoading(false)},[]);useEffect(()=>{load()},[load]);async function change(id:number,estado:string){await fetch("/api/admin",{method:"PATCH",headers:{"content-type":"application/json"},body:JSON.stringify({id,estado})});await load()}return <main className="admin"><header className="adminHead"><div><p className="eyebrow">CHICKEN HUERTA</p><h1>Panel del sorteo</h1><p>Valida comprobantes y controla todos los tickets vendidos.</p></div><a href="/">Ver página pública</a></header><section className="stats"><article><span>Registros</span><strong>{rows.length}</strong></article><article><span>Tickets</span><strong>{rows.reduce((n,r)=>n+r.cantidad,0)}</strong></article><article><span>Ventas aprobadas</span><strong>S/{rows.filter(r=>r.estado==="aprobado").reduce((n,r)=>n+r.monto,0)}</strong></article></section>{loading&&<p className="notice">Cargando registros…</p>}{error&&<div className="notice"><p>{error}</p><a className="primary" href="/signin-with-chatgpt?return_to=%2Fadmin">Iniciar sesión</a></div>}{!loading&&!error&&<div className="tableWrap"><table><thead><tr><th>Participante</th><th>Pago</th><th>Tickets</th><th>Comprobante</th><th>Estado</th><th>Acción</th></tr></thead><tbody>{rows.map(r=><tr key={r.id}><td><b>{r.nombre}</b><small>DNI {r.dni} · {r.celular}</small></td><td><b>S/{r.monto}</b><small>Op. {r.operacion}</small></td><td className="codes">{r.tickets}</td><td><a target="_blank" rel="noreferrer" href={`/api/comprobantes?key=${encodeURIComponent(r.comprobanteKey)}`}>Ver archivo</a></td><td><span className={`status ${r.estado}`}>{r.estado}</span></td><td><div className="actions"><button onClick={()=>change(r.id,"aprobado")}>Aprobar</button><button className="reject" onClick={()=>change(r.id,"rechazado")}>Rechazar</button></div></td></tr>)}</tbody></table>{rows.length===0&&<p className="empty">Todavía no hay registros.</p>}</div>}</main>}
